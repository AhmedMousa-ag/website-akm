export type NavBarProps = {
  // items: string[];
  children: React.ReactNode;
  className: string;
};
export type OpenImperativeHandle = {
  toggle: () => void;
};

export type Props = {};

export type FormInputChildren = {
  title: HTMLLabelElement;
  input: HTMLInputElement;
};
